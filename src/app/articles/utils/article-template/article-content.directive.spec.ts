import { ArticleContentDirective } from './article-content.directive';

describe('ArticleContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ArticleContentDirective();
    expect(directive).toBeTruthy();
  });
});
